# Multidraw canvas app

## Tech stack
- HTML Canvas
- Nextjs
- Express
- Socket.io

## To run
- In client folder run `npm run dev` and in server folder run `npm run server`.
- Open the url `localhost:3000` in multiple browser tabs and enjoy.
